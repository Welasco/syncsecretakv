# Default values for syncsecretakv.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Namespace
namespace: syncsecretakv-system

# Workload Identity
workloadIdentity:
  userAssignedClientId: "xxxxx-xxxx-xxxx-xxxx-xxxxx"